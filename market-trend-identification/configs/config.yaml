# Configuration file

data:
  window_size: 50
  prediction_window: 10
  threshold: 0.05
  symbols:
    - AAPL
    - MSFT
    - GOOGL
    - AMZN
    - TSLA

model:
  test_size: 0.2
  validation_size: 0.15
  random_state: 42
  n_features: 50

paths:
  data_dir: ./data
  models_dir: ./models
  results_dir: ./results
